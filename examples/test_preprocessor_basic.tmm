; Test file for basic preprocessor functionality
; This file tests symbol definition and substitution

.const TEST_VALUE = 42
.let var_value = 100

; Use the symbols
ld a, TEST_VALUE
mv bw, var_value

; Test simple expressions
add a, TEST_VALUE
sub bw, var_value
